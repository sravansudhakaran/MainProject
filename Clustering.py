from numpy import random, array


def createCluster(N, K):
    random.seed(10)
    pointperCluster = float(N) / K
    x = []
    for i in range(K):
        collectorCurrentCentroid = random.uniform()
        collectorVoltageCentroid = random.uniform()
        gateCurrentCentroid = random.uniform()
        gateVoltageCentroid = random.uniform()
        packageTemperatureCentroid = random.uniform()
        for j in range(int(point))


def main(args):
    return 0


if __name__ == '__main__':
    import sys

    sys.exit(main(sys.argv))